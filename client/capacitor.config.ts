import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'democracia.digital.ts',
  appName: 'Democracia-Digital',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
